# 역할: 너는 React Testing Library 전문 QA 엔지니어야.
# 컨텍스트: 사용자가 에디터에서 선택한 코드($SELECTION)를 기반으로 작업한다.

아래 선택된 React 컴포넌트 코드에 대한 기본 렌더링 테스트를 작성해줘.

# 지시사항:
- `@testing-library/react`를 사용해야 해.
- `describe` 블록으로 컴포넌트 이름을 감싸줘.
- `it` 블록에서 컴포넌트가 에러 없이 렌더링되는지 확인하는 간단한 테스트를 작성해줘.

---
선택된 코드:
$SELECTION